{
	"type": "object",
	"properties": {
		"name": {
            "$ref": "api://schema/reuse/name.json"
		},
		"roomType": {
            "enum": ["Sala laboratoryjna", "Sala konferencyjna", "Sala wykładowa"]
		},
		"seatsCount": {
			"type": "integer",
            "minimum": 5,
            "maximum": 1000
        },
		"floor": {
			"type": "integer",
            "minimum": -1,
            "maximum": 20
		},
        "imageId": {
            "$ref": "api://schema/reuse/id.json"
        },
        "buildingId": {
            "$ref": "api://schema/reuse/id.json"
        },
        "blocked": {
            "type": "boolean"
        }
	},
	"required": [],
	"additionalProperties": false
}
