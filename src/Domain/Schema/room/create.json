{
	"type": "object",
	"properties": {
		"name": {
			"$ref": "api://schema/reuse/name.json"
		},
		"roomType": {
            "enum": ["lecture", "laboratory"]
		},
		"seatsCount": {
			"type": "integer",
            "minimum": 5,
            "maximum": 1000
        },
		"floor": {
			"type": "integer",
            "minimum": -1,
            "maximum": 20
		}
	},
	"required": [
		"name",
		"roomType",
		"seatsCount",
		"floor"
	],
	"additionalProperties": false
}
