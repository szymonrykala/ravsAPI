{
    "type": "object",
    "properties": {
        "name": {
            "$ref": "api://schema/reuse/name.json"
        },
        "openTime": {
            "type": "string",
            "format": "time"
        },
        "closeTime": {
            "type": "string",
            "format": "time"
        }
    },
    "required": [
        "name",
        "openTime",
        "closeTime"
    ],
    "additionalProperties": false
}